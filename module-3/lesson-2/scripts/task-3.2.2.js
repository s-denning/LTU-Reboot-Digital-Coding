//  1. Write a simple program to combine a first name and a last name inside a function.

//Then update the function to accept a first and last name as arguments.

var firstName = 'Stan '
var lastName = 'Denning'

function fullName() {
    let fullName = firstName + lastName 
    document.write(fullName)
}

fullName()